# `abm_webapp`

Single-user Dash webapp for running ABM_Uni_v3 simulations with live plots and log tailing.

Run from repo root:

```bash
conda activate main
conda install -c conda-forge dash
python -m abm_webapp.app
```

See `docs/webapp.md` for details.

